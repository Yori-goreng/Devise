<div class="topPage">
  <div class="UserWrapper">
    <% @users.each do |user| %>
      <div class="user">
        <div class="user_header">
          <h2 class="user_header_title"><%= user.title %></h2>
          <div class="user_header_action">
            <%= link_to(content_tag(:i, '', class: 'fas fa-pen'), edit_list_path(list)) %>
          </div>          
        </div>
      </div>
    <% end %>
  </div>
</div>